class Estados {
    constructor(id_estado, lista_estados) {
        this.id_estado = id_estado,
            this.lista_estados = lista_estados
    }

    update() {
        //hace una actualización de estado
    }
}